{
  "name": "market-sentiment-score",
  "private": true,
  "version": "0.1.0",
  "description": "Market sentiment buy/sell score dashboard (VIX, CNN F&G, S&P 500, S5FI) on Cloudflare free tier.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test:unit": "pnpm -r test:unit",
    "test:integration": "pnpm -r test:integration",
    "build": "pnpm -r build"
  }
}
