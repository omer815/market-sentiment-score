{
  "name": "@market-sentiment/cron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions cron runner — fetches market data (TradingView + CNN F&G), scores it, and POSTs the snapshot to the Worker.",
  "scripts": {
    "start": "tsx src/run.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'cron: no lint yet' && exit 0",
    "test:unit": "echo 'cron: no unit tests yet' && exit 0"
  },
  "dependencies": {
    "@mathieuc/tradingview": "3.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
